<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.company.takeyournotes">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.AddNoteActivity"
            android:label="@string/activity_name_new_note"
            android:parentActivityName=".ui.MainActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity
            android:name=".ui.NoteDetailsActivity"
            android:label="@string/activity_name_note"
            android:parentActivityName=".ui.MainActivity" />
        <activity
            android:name=".ui.NoteEditActivity"
            android:label="@string/activity_name_edit_note"
            android:parentActivityName=".ui.MainActivity"/>
    </application>

</manifest>